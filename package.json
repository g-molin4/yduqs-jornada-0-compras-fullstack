{
  "name": "yduqs-jornada-0-compras-fullstack",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend exec -- concurrently -n backend,frontend -c yellow,cyan \"npm run start:dev --prefix backend\" \"npm run dev --prefix frontend\""
  }
}
